@model FiveMinutes.ViewModels.AccountViewModels.AllUsersViewModel
@{
    ViewData["Title"] = "All Users";
}

<div class="container mt-4">
    <div class="row">
        <!-- Left Section: 3/4 Width for Students and Teachers -->
        <div class="col-md-9">
            <h2>Teachers</h2>
            <!-- Search input for Teachers -->
            <input type="text" id="teacherSearch" class="form-control mb-2" placeholder="Search Teachers" onkeyup="filterTable('teacherTable', 'teacherSearch')" />
            <table class="table table-striped" id="teacherTable">
                <thead>
                    <tr>
                        <th>ID</th>
                        <th>Username</th>
                        <th>Email</th>
                        <!-- Add any other relevant columns -->
                    </tr>
                </thead>
                <tbody>
                    @foreach (var teacher in Model.Teachers)
                    {
                        <tr>
                            <td>@teacher.Id</td>
                            <td>@teacher.UserName</td>
                            <td>@teacher.Email</td>
                        </tr>
                    }
                </tbody>
            </table>

            <h2>Students</h2>
            <!-- Search input for Students -->
            <input type="text" id="studentSearch" class="form-control mb-2" placeholder="Search Students" onkeyup="filterTable('studentTable', 'studentSearch')" />
            <table class="table table-striped" id="studentTable">
                <thead>
                    <tr>
                        <th>ID</th>
                        <th>Username</th>
                        <th>Email</th>
                        <!-- Add any other relevant columns -->
                    </tr>
                </thead>
                <tbody>
                    @foreach (var student in Model.Students)
                    {
                        <tr>
                            <td>@student.Id</td>
                            <td>@student.UserName</td>
                            <td>@student.Email</td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>

        <!-- Right Section: 1/4 Width for Admins -->
        <div class="col-md-3">
            <h2>Admins</h2>
            <!-- Search input for Admins -->
            <input type="text" id="adminSearch" class="form-control mb-2" placeholder="Search Admins" onkeyup="filterTable('adminTable', 'adminSearch')" />
            <table class="table table-striped" id="adminTable">
                <thead>
                    <tr>
                        <th>ID</th>
                        <th>Username</th>
                        <th>Email</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var admin in Model.Admins)
                    {
                        <tr>
                            <td>@admin.Id</td>
                            <td>@admin.UserName</td>
                            <td>@admin.Email</td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>

<script>
    function filterTable(tableId, searchInputId) {
        // Get the input field and table elements
        var input = document.getElementById(searchInputId);
        var filter = input.value.toLowerCase();
        var table = document.getElementById(tableId);
        var rows = table.getElementsByTagName("tr");

        // Loop through table rows and hide those that don't match the search query
        for (var i = 1; i < rows.length; i++) { // Start from 1 to skip the header row
            var cells = rows[i].getElementsByTagName("td");
            var match = false;

            // Check each cell in the row for a match
            for (var j = 0; j < cells.length; j++) {
                if (cells[j]) {
                    if (cells[j].innerText.toLowerCase().includes(filter)) {
                        match = true;
                        break;
                    }
                }
            }

            // Show or hide the row based on match status
            rows[i].style.display = match ? "" : "none";
        }
    }
</script>
